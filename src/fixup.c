#include <stdio.h>

#include "fixup.h"

bool fixup_apply(
    uint8_t *record, 
    uint32_t record_size, 
    uint32_t sector_size, 
    uint16_t usa_offset, 
    uint16_t usa_count
)
{
    return true;
}